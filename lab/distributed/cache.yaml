grpc-server:
  address: :50100
  # TLS config
  # tls:
  #   ca:
  #   cert:
  #   key:
  #   skip-verify:
  max-recv-msg-size: 0
  rpc-timeout: 60s
  # buffer-size: 1000000 # default 100k
  # write-workers: 32 # default 16

cache:
  store-type: badgerdb
  dir: /cached/caches
  max-caches: 16
  # max-candidate-per-cache: 100

prometheus:
  address: :50190